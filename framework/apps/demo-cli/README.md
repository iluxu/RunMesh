# Demo CLI

Small streaming CLI that uses RunMesh with a toy tool. Run with:

```
pnpm --filter runmesh-demo-cli start "résume cet article: <lien>"
```

Requires `OPENAI_API_KEY` in env and optional `OPENAI_MODEL` override.
